import 'package:flutter/material.dart';

class LicenzaSoftwarePage extends StatelessWidget {
  const LicenzaSoftwarePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Center(child: Text('Pagina Licenza Software')),
    );
  }
}

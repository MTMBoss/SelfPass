import 'package:flutter/material.dart';

class AssicurazionePage extends StatelessWidget {
  const AssicurazionePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Center(child: Text('Pagina Assicurazione')),
    );
  }
}

import 'package:flutter/material.dart';

class PrevidenzaSocialePage extends StatelessWidget {
  const PrevidenzaSocialePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Center(child: Text('Pagina Previdenza Sociale')),
    );
  }
}

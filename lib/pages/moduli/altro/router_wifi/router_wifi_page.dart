import 'package:flutter/material.dart';

class RouterWifiPage extends StatelessWidget {
  const RouterWifiPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Router Wi-Fi')),
      body: const Center(child: Text('Pagina Router Wi-Fi')),
    );
  }
}

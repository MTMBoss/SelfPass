import 'package:flutter/material.dart';

class FornitoreServiziInternetPage extends StatelessWidget {
  const FornitoreServiziInternetPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Center(child: Text('Pagina Fornitore dei servizi Internet')),
    );
  }
}

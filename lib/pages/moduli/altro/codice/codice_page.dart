import 'package:flutter/material.dart';

class CodicePage extends StatelessWidget {
  const CodicePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Codice')),
      body: const Center(child: Text('Pagina Codice')),
    );
  }
}

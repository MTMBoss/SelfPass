import 'package:flutter/material.dart';

class TuttiPage extends StatelessWidget {
  const TuttiPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Tutti', style: TextStyle(fontSize: 24)));
  }
}

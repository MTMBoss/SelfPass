import 'package:flutter/material.dart';

class ContoBancarioPage extends StatelessWidget {
  const ContoBancarioPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Center(child: Text('Pagina Conto Bancario')),
    );
  }
}
